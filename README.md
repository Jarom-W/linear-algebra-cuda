# linear-algebra-cuda
This repository contains sample code written in C++ and compiled with the Nvidia Cuda Compiler (NVCC) to accelerate common parallel operations such as vector addition and matrix multiplication. 
